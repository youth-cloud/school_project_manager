## 当前项目进程

### 已完成模块
- `edu_class`：已完成基础 CRUD、统一返回、全局异常、分页与条件查询
- `edu_course`：已完成基础 CRUD、分页与条件查询
- `training_batch`：已完成基础 CRUD、关联 ID 校验、时间校验、分页与条件查询
- `project_topic`：已完成基础 CRUD、业务一致性校验、分页与条件查询
- `topic_application`：已完成列表、新增、审核、分页与条件查询
- `project_group`：已完成列表、新增、详情、修改、删除、分页与条件查询
- `project_group_member`：已完成列表、新增、详情、删除、分页与条件查询
- `stage_task`：已完成列表、新增、详情、修改、删除、分页与条件查询

### 当前完成点
- `ProjectGroupMember` 模块接口已全部跑通
- `StageTask` 模块接口已全部跑通
- 数据库缺失业务表已补齐，后续可继续按计划书主线推进

### 关键说明
- 当前数据库真实结构以本地最新 SQL 文件为准
- `project_group_member` 当前按现有表结构使用物理删除
- `stage_task` 当前按现有表结构使用逻辑删除
- `project_group_member` 已补充业务校验：
  - 项目组存在校验
  - 用户存在校验
  - 重复入组校验
  - 组长唯一性校验
- `stage_task` 已补充业务校验：
  - 批次存在校验
  - 教师存在校验
  - 教师与批次绑定关系校验
  - 同批次阶段序号唯一校验

### 下一步
- 优先建议进入 `stage_submission`
- 因为业务主线已经到“教师发布阶段任务”，自然下一步就是“学生/项目组提交阶段成果”