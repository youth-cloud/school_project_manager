## 压缩优先读取区
- 项目方向：学校实训管理平台 / 实训项目管理平台，目标是贴近实战、可演示、可继续迭代、最终可部署到云服务器正常使用，不做随便的课堂 CRUD
- 当前业务口径：`批次 = 单教师 + 单课程 + 单班级 + 单次实训`，不是学院级总批次
- 工作方式：复杂设计与特殊校验走教学模式；高重复 CRUD 可按模块批量推进
- 文档规则：每完成一个大阶段，同时同步更新 `项目进程.md` 和 `可优化项.md`
- 长期记忆源：优先读取 `计划书.md`、`项目进程.md`、`可优化项.md`、`schoolprojectmanager.sql`、`schoolprojectmanagerwithdata.sql`
- 前端风格要求：蓝白色调、精致但不过度花哨，最终收尾时统一删掉教学提示文案、副标题、说明性文字
- 当前阶段：主业务与一轮完整回归测试已完成，进入部署前的性能优化、交互统一、上线准备阶段

## 当前项目进程

### 阶段结论
- 项目已经完成“主业务第一版 + 业务纠偏 + 一轮完整回归测试”，目前进入最终收尾阶段
- 后端主业务、认证鉴权、角色边界、查询范围收口、深层归属校验、真实文件上传下载预览、建组申请方案 C、管理员用户管理第一版均已落地
- 前端后台壳子、登录链路、路由守卫、核心业务页、名称化选择器、阶段提交与附件整合、部分详情加载态、菜单权限控制、页面文案清理均已完成一轮收口
- 当前系统已经具备继续部署和继续迭代基础，不再是课堂级 CRUD demo

### 当前已完成能力总览
- 认证主线已完成：登录、`/me`、修改密码、管理员重置密码、token 刷新、退出登录、旧 token 失效控制
- 权限主线已完成：`ADMIN / TEACHER / STUDENT` 角色边界、登录态恢复、路由守卫、请求自动带 token、主要业务接口按当前登录用户收口
- 业务安全已完成第一轮：查询范围限制、深层对象归属校验、越权拦截、文件路径收口、真实文件白名单与大小限制
- 文件链路已完成第一轮：本地磁盘上传、数据库记录联动、物理文件删除、受控下载、受控预览、未登录拦截
- 管理员运维能力已补齐关键缺口：可以直接在前端管理教师和学生账号，不再依赖手改数据库
- 全站核心业务页已完成一轮“去 ID 化”和名称化展示，教师统一显示真实姓名，学生统一显示 `真实姓名(用户名)`

### 已完成前端模块
- `notice`：公告管理第一版已完成
- `stage_task`：阶段任务第一版已完成，已支持按批次筛选
- `stage_submission`：阶段提交第一版已完成，详情页已整合附件管理
- `submission_file`：提交附件第一版已完成，已接真实文件链路
- `review_record`：评审记录第一版已完成
- `score_record`：成绩记录第一版已完成，已强制绑定项目组，学生选择已收紧为组内成员
- `weekly_report`：周报第一版已完成
- `defense_schedule`：答辩安排第一版已完成
- `defense_record`：答辩记录第一版已完成，答辩安排展示已改为“项目组 + 时间 + 地点”
- `topic_application`：选题申请第一版已完成
- `project_topic`：课题管理第一版已完成，难度已统一为 4 档
- `project_group`：正式项目组第一版已完成，组 ID 复制与加入链路已打通
- `project_group_application`：建组申请第一版已完成，已支持单人先建组
- `project_group_member`：项目组成员第一版已完成
- `training_batch`：实训批次第一版已完成
- `edu_class`：班级管理第一版已完成
- `edu_course`：课程管理第一版已完成
- `operation_log`：操作日志第一版已完成
- `sys_user`：管理员用户管理第一版已完成

### 已完成后端模块
- `auth`：登录、当前用户、改密、管理员重置密码、refresh、logout、token 生命周期控制已完成
- `system`：`sys_role`、`sys_user_role`、`sys_user` 基础体系与用户管理第一版已完成
- `edu_class`：CRUD、分页与权限范围已完成
- `edu_course`：CRUD、分页与权限范围已完成
- `training_batch`：CRUD、分页、时间字段校验与权限范围已完成
- `project_topic`：CRUD、分页、课题发布教师边界与范围收口已完成
- `topic_application`：申请、审核、分页、按角色视图范围已完成
- `project_group`：正式项目组结果层已完成，且已按方案 C 收口主流程
- `project_group_application`：学生申请建组、教师审核、审核通过自动生成正式项目组已完成
- `project_group_member`：成员管理第一版已完成
- `stage_task`：CRUD、分页、状态与批次关联校验已完成
- `stage_submission`：CRUD、分页、提交链路校验与查询范围已完成
- `submission_file`：元数据接口、真实上传、下载、预览、删除联动已完成
- `review_record`：CRUD、分页、枚举校验、权限范围已完成
- `score_record`：CRUD、分页、项目组必选、学生必须属于项目组校验已完成
- `weekly_report`：CRUD、分页、权限范围已完成
- `defense_schedule`：CRUD、分页、时间类型修正、权限范围已完成
- `defense_record`：CRUD、分页、深层校验与权限范围已完成
- `notice`：CRUD、分页、查询范围收口已完成
- `operation_log`：CRUD、分页、按管理员或本人视图范围收口已完成

### 本阶段最重要的架构成果
- `Spring Boot 4 + Jackson 3` 下的 Long ID 精度问题已定位并修复，前后端雪花 ID 传输保持稳定
- 项目组主流程已升级为方案 C：学生发起建组申请，教师审核，通过后系统自动生成正式项目组
- 建组链路已进一步产品化：申请人默认成为组长，其他成员可通过输入项目组 ID 直接加入
- 正式项目组页已经从“主流程建组页”收口成“结果层管理页”
- 阶段提交与提交附件已在前端交互上整合到同一详情视图
- 管理员用户管理第一版已上线，解决了未来云服务器部署后无法在前端创建教师/学生账号的问题

### 最近完成的重要工作
- 修复课题难度显示与校验不一致问题，前后端统一为 4 档难度
- 修复建组申请审核时误判“存在其他待审核申请”的问题
- 修复学生看不到同批次阶段任务的问题，并统一教师姓名展示口径
- 完成全站主要业务页的名称化展示收口，清理了大量 `教师ID / 学生ID / 发布人ID / 操作人ID`
- 把成绩记录改成项目组必选，并将学生选择收紧为所选项目组内成员
- 调整答辩记录表单中答辩安排的展示文案，使其以项目组为主识别
- 输出并落地 `全流程回归测试清单.md`，并完成一轮完整业务回归测试

### 回归测试结果
- 管理员、教师、学生三端主链路已按回归清单完整跑通
- 选题申请、建组申请、正式项目组、阶段任务、阶段提交、附件、评审、答辩、成绩等主流程已验证通过
- 本轮回归中发现的业务问题已修复，包括：
- 答辩记录下拉展示不直观
- 成绩记录学生下拉未限制为组内成员
- 课题难度 `困难` 无法保存
- 当前回归结论：主业务可继续进入部署前收尾，不再存在阻塞级业务 bug

### 当前代码状态
- 后端目前已经形成：认证层 + 权限层 + 业务层 + 文件链路 + 管理员运维入口 的完整基础闭环
- 前端目前已经形成：后台布局 + 登录链路 + 业务页模板 + 表单弹窗 + 详情弹窗 + 角色控制 + 路由菜单体系 的完整基础闭环
- 业务规则经过本轮讨论后已明确：当前 `批次` 代表单教师、单课程、单班级的一次实训批次，现有主流程在该定义下成立

### 当前仍未做但已明确的内容
- 还没有进入最终部署实操
- 还没有把所有前端模块统一到同一套详情 loading / skeleton / 空态体验
- 还没有系统性处理前端详情首屏串行等待与查询性能问题
- 还没有把部分后端返回从实体统一整理为 VO 展示层

### 下一阶段主线
- 先做前端性能与加载体验优化，重点处理列表首屏、详情弹窗、选择器数据加载、空白等待问题
- 再做一轮前端交互统一：详情 loading、skeleton、empty、按钮布局、卡片头部样式统一
- 然后进入部署准备：环境变量、数据库初始化、打包方式、文件目录、云服务器部署步骤
- 部署前继续做少量非结构性收尾：页面零碎文案、少量展示细节、少量接口返回层整理

### 当前结论
- 当前项目已经完成“第一版可演示、可继续迭代、具备部署基础”的目标
- 回归测试已经证明主业务链路基本稳定，下一步最值得投入的是性能与交互一致性，而不是再新增基础 CRUD 模块
- 如果性能与详情加载体验再统一一轮，这个项目的演示观感和部署完成度会明显再上一个台阶
